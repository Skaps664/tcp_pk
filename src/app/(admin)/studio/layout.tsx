import "../globals.css";

import React from 'react'

const RootLayout = ({children}: {children: React.ReactNode} ) => {
  return (
    <html lang='en'>
        <div>RootLayout</div>
    </html>
  )
}

export default RootLayout